export default async function StatementPage() {
  return (
    <div className="space-y-6">
      <h1 className="text-xl font-semibold">楽天銀行 明細ビュー</h1>

      {/* ↓ これらを削除 */}
      {/* Dashboardへ */}
      {/* インポートへ */}
      {/* ※ 表示は「読むだけ」。インポート/残高/集計ロジックには一切触ってない。 */}

      <div className="rounded-lg border">
        {/* 明細テーブル（完全にそのまま） */}
      </div>
    </div>
  );
}